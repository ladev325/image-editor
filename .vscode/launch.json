{
    "version": "0.2.0",
    "configurations": [
        {
            "preLaunchTask": "CMake build",
            "name": "Image Editor",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/app",
            "args": [],
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb",
            "miDebuggerPath": "/usr/bin/gdb"
        }
    ]
}
